const fs = require('fs'); let c = fs.readFileSync('src/lib/db/schema.ts', 'utf8'); c = c.replace('import { sqliteTable, text, integer } from 'drizzle-orm/sqlite-core';', 'import { pgTable, text, integer, boolean } from 'drizzle-orm/pg-core';'); c = c.replace(/sqliteTable/g, 'pgTable'); c = c.replace(/integer\('is_active', \{ mode: 'boolean' \}\)/g, 'boolean('is_active')'); fs.writeFileSync('src/lib/db/schema.ts', c);
